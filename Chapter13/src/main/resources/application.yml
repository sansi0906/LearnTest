server:
  port: 8080

logging:
  path: logs
  file: userManager.log

spring:
  application:
    name: userManaager
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/my_test?serverTimezone=GMT
    username: root
    password: sansi0906

mybatis:
  type-aliases-package: com.sansi.mode
  mapper-locations:
    - mapper/*